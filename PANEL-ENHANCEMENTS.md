# PANEL ENHANCEMENTS — backlog for the window router (F8) + subscriptions launcher (F9)
**TRK-2026-9785 (admin band) · owner asked 2026-09-14 "how far can I take this" + "can a search field be added that opens to the artifact." Menu of options; pick from it. Both panels stay MATCHED (one identity set).**

## Owner-requested
- **Search field** — (a) filter-as-you-type narrows the buttons; (b) search-and-open: type a TRK/address/name →
  opens that job portal or artifact. Needs a small index of artifacts + job portals behind it. **Cloud-buildable.**

## Quick wins (Cloud, offline, no new plumbing)
- Copy paste-tag on every button.
- Pinned / most-used row (floats top apps up; RAMBO supplies real usage order).
- Manual light/dark toggle (currently follows system).

## Higher value (needs LIVE DATA — Cloud wires the artifact runtime capability)
- **Live status strip:** active seat · loop green/red · OCR % · **count of RED items waiting for owner click** — read from the register.
- "Needs your one click" badge with count.
- "Open my morning set" — one tap opens the day's starting windows (RAMBO wires the launches).

## Needs RAMBO (machine side)
- Focus an already-open window instead of opening a second copy.
- Keep the physical deck + tray + F-keys matched to the artifacts.

## Cloud's recommendation
**Highest daily impact: the search box + the live "needs-your-click" strip.** Search gets you anywhere in one
type; the strip makes the panel TELL you what's waiting instead of you hunting.

## Honest note
The live-data items need the artifact runtime capability (or RAMBO writing a small JSON the panel reads) — a bit
more plumbing than the static buttons. Everything else is quick. Nothing here stores a secret.

#panel-enhancements #search-field #live-status #TRK-2026-9785
`TRK-2026-9785 · v1 · p001 · 2026-09-14 · CURRENT`
