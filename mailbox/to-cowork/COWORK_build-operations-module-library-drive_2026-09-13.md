# 🤝 COWORK — build the OPERATIONS MODULE LIBRARY in Google Drive (A-to-Z) + re-scout the turnkey program
**2026-09-13 · owner directive · architecture = OPERATIONS-ARCHITECTURE_SOURCE-OF-TRUTH.md (TRK-2026-9784). Owner wants Cowork (not Code) to build this, and to watch it go A-to-Z.**

**WHY YOU:** the source of truth lives in **Google Drive** (every seat reaches it; the git repo Cowork can't).
You can read/write Drive — so you build the library there.

**JOB 1 — build the module library in Drive `VTES-OPERATIONS\`:** one block per thing, same shape as
`LLM-SUBSCRIPTIONS.md`. Cover:
- **Programs / tools / connectors** we use (what it is, where it lives, how to run it, owner-notes).
- **Communication methodology** (the seats, the mailbox lanes, paste-ID system, RED/GREEN, end-with-a-question rule).
- **Contacts / subscriptions** (link the LLM subscriptions sheet + key vendor contacts).
- Each module: title + TRK + `CANONICAL: Drive` + hashtags. One canonical copy — do NOT create a second
  hand-edited tree that competes with the repo; where something already lives in the repo, LINK it, don't fork it.

**JOB 2 — re-scout the turnkey infrastructure program:** owner recalls two off-the-shelf options — one
**superior but harder to set up**, one **inferior but easier**. **Neither is recorded in our files.** Research
both (current options for a self-hostable "plug in your modules / agent-orchestration" platform), present them
with the easy-vs-superior tradeoff + setup effort + cost, and recommend. **Do NOT install anything** — installation
is RAMBO's (only RAMBO touches the machine); you produce the pick + install plan.

**RETURN:** build in Drive directly; drop a short status + the turnkey recommendation to `mailbox/to-cloud/` (or
email Jorge) so Cloud/RAMBO can bridge. **Filing/moving client files and installing = not yours.**

**CLOSING QUESTION:** Is the Drive `VTES-OPERATIONS` module library taking shape, and which turnkey option do you recommend — the easier one, or the superior one if RAMBO can install it hands-off?

#cowork #operations-modules #google-drive-source-of-truth #turnkey-rescout #TRK-2026-9784
