# HANDOFF — ☁️ CLOUD → 🖥️ RAMBO: OneDrive backup of the ops library + install the chosen turnkey program
**2026-09-13 · architecture = OPERATIONS-ARCHITECTURE_SOURCE-OF-TRUTH.md (TRK-2026-9784). Machine parts only RAMBO can do.**

**JOB 1 — OneDrive BACKUP (GREEN):** once Cowork's `VTES-OPERATIONS\` module library exists on Google Drive,
set a **one-way backup/mirror to OneDrive** (backup only — NOT a second editable tree; the Drive copy is canonical).
Log the sync job so it's re-runnable and so a failure is visible.

**JOB 2 — install the turnkey infrastructure program (RED gate on the choice, GREEN mechanics):**
- Cowork is re-scouting the two options (superior/harder vs inferior/easier). **When the pick is in:**
- **Owner's decision rule:** if you (RAMBO) can install the **superior** one **with NO owner intervention**
  (no owner clicks, no credential entry, no spend past caps), do that. **Otherwise install the easier one.**
- Assess honestly which is hands-off-installable and say so; don't force the superior one through an owner click
  unless Jorge approves. Install is the deliverable; a plan describing it is not.
- **Never** blind-install outside-built code (Rule 10) — review first. No spend past OD-BUDGET-01 without a click.

**CLOSING QUESTION:** Can the superior turnkey program be installed hands-off (no owner intervention) — yes/no — and is the OneDrive backup of the ops library running?

#onedrive-backup #turnkey-install #rambo-hands #rule-10 #TRK-2026-9784 #cloud-to-desktop
