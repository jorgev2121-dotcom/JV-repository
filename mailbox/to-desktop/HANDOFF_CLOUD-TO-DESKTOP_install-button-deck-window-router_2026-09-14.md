# HANDOFF — ☁️ CLOUD → 🖥️ RAMBO: install an off-the-shelf BUTTON DECK as the window/subscription router (don't build one)
**2026-09-14 · owner wants a proven off-the-shelf mini control panel — big labeled buttons so he knows exactly which window he's pasting into. Cloud researched; do NOT hand-build.**

**RECOMMENDED (software, free to start): Touch Portal.** Turns a spare phone/tablet into a labeled button
deck; free, or $14 one-time for up to 110 buttons/page; each button launches/focuses an app or opens a URL.
Best fit for "know what I'm pasting into" with no clutter on the main screen. (Mature, widely used.)
**Hardware alternative if he wants physical keys:** VSDINSIDE M18 (~$60, 18 keys) · Razer Stream Controller X
(~$80) · Elgato Stream Deck (~$150). Rainmeter is the free on-screen-panel route but is more DIY.

**EXACT NEXT ACTION:**
1. **Confirm current version/price + that it can FOCUS a specific window** (not just launch) on Jorge's Windows —
   verify before relying; if window-focus is weak, note it and fall back to launch + AutoHotkey focus.
2. **Install the Touch Portal PC helper** (Jorge installs the phone/tablet app himself). Or, if he chose hardware,
   set that up.
3. **Wire the buttons to match the two artifacts** (same identities, so the physical panel and the on-screen ones agree):
   - 💬 Chat (PASTE-X) · 🤝 Cowork (PASTE-X) · 🖥️ Code·Desktop/RAMBO (PASTE-D) · ☁️ Code·Cloud/Web (PASTE-C)
   - a row for the AI subscriptions (Claude / ChatGPT / Gemini / Grok) that opens each.
   - **Label each button with its emoji + paste-tag** so the physical button says exactly what it is.
4. Record the config so it's reproducible; log it.

**RED or GREEN:** installing the free helper + configuring buttons = GREEN. **Any purchase (the $14 unlock, or a
hardware deck) = RED — owner's one click, within OD-BUDGET-01.** Never a skip-all-permissions mode.

**CLOSING QUESTION:** Does Touch Portal focus a named window reliably on his machine — and did the buttons come up labeled with the right emoji + paste-tag?

#button-deck #touch-portal #off-the-shelf #window-router #no-build #paste-tag #RED-on-spend #cloud-to-desktop
