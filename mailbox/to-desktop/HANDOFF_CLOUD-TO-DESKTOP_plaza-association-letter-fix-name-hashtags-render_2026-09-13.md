# HANDOFF — ☁️ CLOUD → 🖥️ RAMBO: fix the Plaza Association letter — no name, no hashtags, opens as raw HTML
**2026-09-13 · Plaza job TRK-2026-1582 · file "Plaza draft letter (TRK-1582-LC)" · owner: "no name on file, no hashtags, opens to HTML but should be with text and images."**

**CONTEXT (Cloud already checked):** dedupe is CLEAR — no duplicate and NOT sent in Gmail/Outlook/Drive
(only a sent 7/10 window thread + a bounced Unit 220/721 extension, both different). So there's one draft;
it just has three defects. The file lives on the desktop — Cloud can't edit it, it's yours.

**THE THREE DEFECTS TO FIX (same class as the Alec portal's blank header):**
1. **No name on file** → stamp the identity: **TRK-2026-1582** + a real document title + the footer stamp
   per charter §9 (`TRK-2026-1582 · v · pNNN · date · CURRENT`), so the letter carries its own name.
2. **No hashtags** → add category handles in the BODY/metadata (never filename): `#Plaza-of-Bal-Harbour`
   `#c-plaza-association` `#p-qmarte` `#j-balharbour` `#TRK-2026-1582` — so it's findable and cross-referenced.
3. **"Opens to HTML but should be text + images"** → the outbound artifact should be a **rendered document,
   not raw HTML source.** RECOMMENDED: export the letter to a **clean PDF** (or Word) with the per-unit
   status text AND the images laid in inline; keep the HTML only as the source. If it must stay HTML, embed
   the images (data-URI) and confirm it RENDERS (Chrome default-app fix from the other handoff) rather than
   showing code. A letter to an Association reads as a document, not a web page.

**ALSO:** confirm there's a **single** desktop copy (no second draft) — Cloud confirmed no dupe in email/Drive
but can't see the desktop.

**RED or GREEN:** fixing the file (name, hashtags, render, PDF export) = GREEN. **Sending it to the
Association stays the owner's decision (RED)** — do NOT send; produce the clean version for his review.

**CLOSING QUESTION:** Once you stamp TRK-2026-1582 + hashtags and export a clean text+images PDF, is there a single desktop draft, and is it ready for Jorge's send decision?

#plaza #TRK-2026-1582 #association-letter #no-name #no-hashtags #render-text-images #pdf-not-raw-html #cloud-to-desktop
