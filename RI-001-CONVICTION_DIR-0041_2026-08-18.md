# RI-001 — CONVICTED. Two tasks caught by name, and the fix was already on the machine.

**TRK-2026-9331 · DIR-0041 · desktop capture 10:51–10:54 ET · mirrored + peer-reviewed by the
manager window 2026-08-18 15:30 UTC · Jorge holds the STOP THEM button.**

---

## The conviction, upgraded from this morning's lead

| Time | Caught | Locked out |
|---|---|---|
| 10:53:44 | **CU-Bus-Dispatcher** → `pwsh -WindowStyle Hidden … Bus-Dispatcher.ps1` | **2.9 s** |
| 10:54:05 | **CU-Records-Watch** → `powershell -WindowStyle Hidden … Watch-RecordsDelivery.ps1` | **4.3 s** |

**The decisive technical fact: `-WindowStyle Hidden` does not prevent the console window's
creation.** It is created, takes the foreground, *then* hides. Every one of the twelve
repairs that added that flag was applying a fix that cannot work — **which is the final
missing piece of the 24-month story.**

**Not Claude, proven by timing:** twelve steals logged before this session's first tool call.

**Fleet census: 51 tasks launch a shell directly. 19 use the `Run-Hidden.vbs` wrapper — and
during the capture, every wrapper task fired and took zero foreground.** The control group
was running in production the whole time.

## Peer review, per PEER-REVIEW-02

**STRONGEST-OBJECTION:** fixing 4 of 51 leaves 47 tasks with the identical launch pattern —
the hourly and 30-minute ones still steal several times a day, so "significantly better then
creeps back" is a real risk *if the remaining 47 are forgotten again.*
**ONE-IMPROVEMENT:** the logger stays running after the fix; tomorrow's table must show the
:03 / :23 / :44 families GONE — the before/after is the proof — then batch the remaining 47
under the same wrapper as pass two, already scoped.
**VERDICT: PROCEED.** The 4-task scope is right: 90% of the interruptions, four actions,
trivially reversible. Wrapping is **Tier 2 — the console never exists** — so Rule 4 is
satisfied, not violated. The UAC prompt is Jorge clicking Yes on a change he ordered.
**MEASURED:** the capture's own control group (19 wrapper tasks, 0 steals) — not reasoning.

## One more RI-025 instance, filed

*"0 of 231 tasks flash a window"* from 2026-08-17 measured **which wscript calls lack
`//B`** — never **how many tasks launch a shell directly** (51). A true number, describing a
rule nobody stated. **That line is retired and must not be quoted again.**

## The proof loop after Jorge clicks

1. STOP THEM re-points 4 tasks at the proven wrapper (his Yes on the UAC prompt).
2. Logger keeps running — **tomorrow's interval table must show the two families gone.**
3. Then pass two: the remaining 47, batch-wrapped, same verification.
4. RI-001 closes only on that before/after table — **not on the click.**

#RI-001 #TRK-2026-9331 #DIR-0041 #Convicted #JorgeValdes #CU-Inspections
