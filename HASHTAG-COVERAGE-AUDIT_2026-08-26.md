# HASHTAG COVERAGE AUDIT — every capsule to ≥5 hashtags (findability rule)
**2026-08-26 · from the "Orange Tree" lesson: the remembered name must be a hashtag ON the file, not only on the index above it. #hashtag-audit #JorgeValdes #CU-Inspections**

## THE LESSON (why this matters)
A file named `_CAPSULE-REPORT_Plaza-305_TRK-2026-1582-PZ305` was invisible to every search for "Orange Tree,"
because only the INDEX above it carried that name. Same failure as the county's "bldg jacket" vs everyone's
"tax jacket." **Fix: put the remembered/common name as a hashtag ON the actual documents, not just the folder.**

## THE ≥5-HASHTAG STANDARD (apply to every capsule)
Each capsule must carry at least 5 hashtags, drawn from:
1. `#JorgeValdes` (baseline — already on all)
2. `#CU-Inspections` (baseline — already on all)
3. `#<TRK-number>` — the tracking number itself as a tag (e.g. `#TRK-2026-1534`)
4. `#<client-or-owner>` — the client/owner name (e.g. `#Alec-Valdes`)
5. `#<address-or-development-name>` — the street address AND any REMEMBERED/common name (e.g. `#Orange-Tree`, `#1840-NW-63-St`)
Add `#<doc-type>` / `#<area>` where useful. **The remembered name is the highest-value tag — it's what Jorge searches.**

## CURRENT STATE (top-level hashtags counted from CAPSULE-INVENTORY_2026-08-26)
Every capsule already carries the 2 baseline tags. **34 of 41 capsules fall UNDER 5 total** and need top-up.
The 7 already at/above 5 (leave as-is): TRK-2026-1612, _ALEC-VALDES-DD, TUS-26-1033 (Medley),
TRK-2026-0708-JULIA, TRK-2026-1262, OPH-2026-0007, and the Homestead TRK-TBD.

**The other 34 need at least one to three more tags each** to reach 5 — add the TRK-as-tag, the address, and
the remembered/common name, pulled from each capsule's own folder. Worst-covered (only 1 specific tag today,
need 2 more): 1286, 1514, 1310, 1588, 1268, 1535, 1536, 1280, TUS-26-1018 (Pasadena), 15601-TBD.

## HOW TO APPLY (desktop, GREEN — sidecars/index, not client originals)
Per Rule 5, fan out one worker per capsule. For each: read the folder, add the missing hashtags to the
capsule's `.TAGS.txt` / index / portal (NOT by editing client original documents). Confirm the count reached ≥5.
Report a denominator: "X of 41 capsules now ≥5 hashtags."

---
*Findability = the remembered name is a hashtag on the file. Every capsule to ≥5. #hashtag-audit*
